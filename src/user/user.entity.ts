/* eslint-disable prettier/prettier */
// src/users/user.entity.ts// Definir una clase llamada 'User'
export class User {
    // Propiedad 'id' de tipo número para almacenar la identificación del usuario
    id: number;
  
    // Propiedad 'username' de tipo cadena para almacenar el nombre de usuario del usuario
    username: string;
  
    // Propiedad 'email' de tipo cadena para almacenar la dirección de correo electrónico del usuario
    email: string;
  }
  